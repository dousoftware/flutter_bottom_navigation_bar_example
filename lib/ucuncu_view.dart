import 'package:flutter/material.dart';

class UcuncuView extends StatelessWidget {
  const UcuncuView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 800,
      width: 500,
      color: Colors.green[200],
      child: const Center(child: Text("Ucuncu View")));
  }
}
